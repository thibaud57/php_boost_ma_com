{% extends 'base.html.twig' %}

{% block title %}Edit Customers{% endblock %}

{% block body %}
    <h1>Editer un client</h1>

    {{ include('customers/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    {{ include('customers/_delete_form.html.twig') }}

    <button onclick="location.href='{{ path('app_register', {'id': customer.getId()})}}'" type="button" class="btn btn-square btn-info">Rendre utilisateur</button>
    <br>
    <a href="{{ path('customers_index') }}">Retour</a>

{% endblock %}
