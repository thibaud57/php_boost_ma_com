{% extends 'base.html.twig' %}

{% block title %}Rqst{% endblock %}

{% block body %}
    <h3>Demandes n° {{ rqst.id }}</h3>
    <table class="table">
        <tbody>
            <tr>
                <th>Object</th>
                <td>{{ rqst.object }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ rqst.content }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ rqst.status ? rqst.status|join(', ') : '' }}</td>
            </tr>
        </tbody>
    </table>


    <button onclick="location.href='{{ path('rqst_edit', {'id': rqst.id}) }}'" type="button" class="btn btn-square btn-outline-warning">Editer</button>
    {{ include('rqst/_delete_form.html.twig') }}

    <a href="{{ path('rqst_index') }}">Retour</a>

{% endblock %}
