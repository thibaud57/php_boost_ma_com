{% extends 'base.html.twig' %}

{% block title %}Rqst{% endblock %}

{% block body %}
    <h1>Rqst</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rqst.id }}</td>
            </tr>
            <tr>
                <th>Object</th>
                <td>{{ rqst.object }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ rqst.content }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ rqst.status ? rqst.status|join(', ') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('rqst_index') }}">back to list</a>

    <a href="{{ path('rqst_edit', {'id': rqst.id}) }}">edit</a>

    {{ include('rqst/_delete_form.html.twig') }}
{% endblock %}
