{% extends 'base.html.twig' %}

{% block title %}Tickets{% endblock %}

{% block body %}
    <h1>Ticket n° {{ ticket.id }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Objet</th>
                <td>{{ ticket.object }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ ticket.content }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ ticket.status ? ticket.status|join(', ') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <button onclick="location.href='{{ path('tickets_edit', {'id': ticket.id}) }}'" type="button" class="btn btn-square btn-outline-warning">Editer</button>
    {{ include('tickets/_delete_form.html.twig') }}

    <a href="{{ path('tickets_index') }}">Retour</a>
{% endblock %}
